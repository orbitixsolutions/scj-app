export type WorkshopPageProps = {
  params: {
    id: string
  }
  searchParams: {
    firstName: string
    lastName: string
  }
}

export type WorkshopStudentActionProps = {
  firstName: string
    lastName: string
}
